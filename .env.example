# Backend
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXX
PORT=4242

# Frontend client base URL for redirect URLs
CLIENT_BASE_URL=http://localhost:3000

# Database
# For local MongoDB, you can use: mongodb://127.0.0.1:27017/checkout
# For MongoDB Atlas, paste your connection string here (do NOT commit real creds):
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/checkout?retryWrites=true&w=majority&appName=<appName>

# Admin bootstrap (optional)
# On server start, if this user does not exist, it will be created as admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# JWT settings
JWT_SECRET=change_this_dev_secret
JWT_EXPIRES_IN=7d
